apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-saddle.fullname" . }}
  labels:
    {{- include "rancher-saddle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rancher-saddle.selectorLabels" . | nindent 4 }}
